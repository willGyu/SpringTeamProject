<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-4.3.xsd
		http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- 객체(빈)을 미리 생성하는곳 (단, 웹에 관한 정보는 제외) -->
	
	<!-- hikariConfig : 디비 연결정보(CP) -->
	<bean id="hikariConfig" 
	      class="com.zaxxer.hikari.HikariConfig">
	      
	      <!-- 원본
	       <property name="driverClassName"  value="com.mysql.cj.jdbc.Driver"/>
	       <property name="jdbcUrl" value="jdbc:mysql://localhost:3306/springdb"/>
	       -->
	      
	      <property name="driverClassName" 
	                value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy"/>
	      <property name="jdbcUrl" 
	                value="jdbc:log4jdbc:mysql://localhost:3306/springdb"/>
	      
	      <property name="username" value="root"/>
	      <property name="password" value="1234"/>		
	</bean>
	<!-- hikariConfig : 디비 연결정보(CP) -->
	
	<!-- dataSource : 디비 연결정보 (hikari CP설정), 자원해제 -->
	<bean id="dataSource" 
	      class="com.zaxxer.hikari.HikariDataSource" 
	      destroy-method="close" >
	      
	      <constructor-arg ref="hikariConfig"/>
	      
	</bean>
	<!-- dataSource : 디비 연결정보 (hikari CP설정), 자원해제 -->
	
	<!-- sqlSessionFactory : 디비연결, 자원해제, SQL구문 실행,
	                         mybatis 설정,mapper 설정 -->
	<bean id="sqlSessionFactory" 
		  class="org.mybatis.spring.SqlSessionFactoryBean">
		  
		  <property name="dataSource" ref="dataSource"/>
		  <property name="configLocation" 
		            value="classpath:/mybatis-config.xml"/>
		  <!-- <property name="mapperLocations" 
		            value="classpath:/mappers/**/*Mapper.xml"/> -->
	</bean>
	<!-- sqlSessionFactory : 디비연결, 자원해제, SQL구문 실행,
	                         mybatis 설정,mapper 설정 -->
	                         
	<!-- sqlSession : 디비연결(자동), 자원해제(자동), SQL구문 실행,
	                  mybatis 설정,mapper 설정,  -->               
	<bean id="sqlSession" 
	      class="org.mybatis.spring.SqlSessionTemplate"
	      destroy-method="clearCache" >
		
		<constructor-arg ref="sqlSessionFactory"/>
	</bean>  
	<!-- sqlSession : 디비연결(자동), 자원해제(자동), SQL구문 실행,
	                  mybatis 설정,mapper 설정,  -->    
	                  
	<!-- 객체를 찾기(외부에서 선언된 @Repository,@Service 이 붙어있는 객체)  -->
	<context:component-scan base-package="com.itwillbs.persistence" />                                       
	<context:component-scan base-package="com.itwillbs.service" />                                       
	
	
		
</beans>
